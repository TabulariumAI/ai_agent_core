import * as Entities from "../../../core/entities/imports";

export const autoRecordChoice: Entities.Choice = {
    items: [
        {
            service: 'Recognition',
            level: 3
        }
    ]
}