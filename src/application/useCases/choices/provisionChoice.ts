import * as Entities from "../../../core/entities/imports";

export const provisionChoice: Entities.Choice = {
    items: [
        {
            service: 'Recognition',
            level: 3
        }
    ]
}